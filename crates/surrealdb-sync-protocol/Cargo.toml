[package]
name = "surrealdb-sync-protocol"
version = "0.1.0"
edition.workspace = true
license = "Apache-2.0"
rust-version.workspace = true
publish = false

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
