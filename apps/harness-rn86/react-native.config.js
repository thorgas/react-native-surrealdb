const {
  configureProjects,
} = require('react-native-test-app');
const {
  createReactNativeConfig,
} = require('../harness-shared/react-native-config');

module.exports = createReactNativeConfig({ configureProjects });
